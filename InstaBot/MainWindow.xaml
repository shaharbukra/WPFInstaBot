<Window x:Class="InstaBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InstaBot.Objects"
        xmlns:helpers ="clr-namespace:InstaBot.Helpers"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:InstaBot"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
         Background="{DynamicResource MaterialDesignPaper}"
         FontFamily="{DynamicResource MaterialDesignFont}"
        >
    <Window.Resources>
        <vm:Bot x:Key="viewmodel" />
    </Window.Resources>
    
    <Grid DataContext="{StaticResource viewmodel}">
    
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="77.507"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.RowSpan="2">
            <Expander ExpandDirection="Right">
                <Expander.Header>
                    <TextBlock Text="Login" RenderTransformOrigin=".5,.5">
                        <TextBlock.LayoutTransform>
                            <RotateTransform Angle="90" />
                        </TextBlock.LayoutTransform>
                    </TextBlock>
                </Expander.Header>
                <StackPanel Orientation="Vertical"
                            TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                            Margin="8,24,16,24">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Grid.Row="1" Grid.Column="0" Kind="Account" Foreground="{Binding ElementName=NameTextBox, Path=BorderBrush}" />
                        <TextBox materialDesign:HintAssist.Hint="Login" Width="150" >
                            <TextBox.Text>
                                <Binding Path="Username" UpdateSourceTrigger="PropertyChanged">

                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Grid.Row="6" Grid.Column="0" Kind="Key"
                                 Margin="0,4,0,4"
                                 Foreground="{Binding ElementName=FloatingPasswordBox, Path=BorderBrush}" />
                        <PasswordBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Password" Width="150" helpers:PasswordHelper.Attach="True" helpers:PasswordHelper.Password="{Binding Password}" >
                        </PasswordBox>
                       
                    </StackPanel>
                    <Button Margin="0,4,0,4" Style="{StaticResource MaterialDesignRaisedLightButton}"  ToolTip="Login" Click="Button_Click">
                        Login
                    </Button>
                </StackPanel>
            </Expander>
        </StackPanel>

        <!--<Button Content="Button" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <TextBox HorizontalAlignment="Left" x:Name="textbox" Height="23" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120" Margin="0,38,0,0"/>
        <Image HorizontalAlignment="Left" Height="100" Margin="287,10,0,0" VerticalAlignment="Top" Width="100" x:Name="profilePicture" Source="{Binding LoggedInUserImage}" >
        </Image>
        <ListBox x:Name="listBox" HorizontalAlignment="Left" Height="196" Margin="20,99,0,0" VerticalAlignment="Top" Width="100" MouseLeftButtonUp="ListBox_OnMouseLeftButtonUp">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding thread_title}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name="listBox1" HorizontalAlignment="Left" Height="196" Margin="154,99,0,0" VerticalAlignment="Top" Width="341">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding text}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->

    </Grid>
</Window>
